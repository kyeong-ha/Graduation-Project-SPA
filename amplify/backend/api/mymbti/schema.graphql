type InputTable @model @auth(rules: [{ allow: public, provider: iam }]) {
    id: ID!
    question: [String!]
    answer: [String!]
}

type MBTITable @model @auth(rules: [{ allow: public, provider: iam }]) {
    mbti: String!
    type: String!
    mind: String!
}