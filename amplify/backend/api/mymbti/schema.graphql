type Input @model @auth(rules: [{ allow: public, provider: iam }]) {
  id: ID!
  question: String!
  answer: String!
}

type MBTI @model @auth(rules: [{ allow: public, provider: iam }]) {
  mbti: String!
  type: String!
  mind: String!
}